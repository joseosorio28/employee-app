server:
  port: 8080

spring:
  application:
    name: "EmployeeApplication"
  devtools:
    add-properties: false

springdoc:
  show-actuator: true

repository:
  endpoint: "https://dummy.restapiexample.com/api/v1"
  resource: "/employee/"
  resources: "/employees"